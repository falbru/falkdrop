info:
  name: Get drop
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/drop/{{?dropId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
