info:
  name: Create drop
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/drop"
  body:
    type: json
    data: |-
      {
        "resource_ids": [
          "{{?resourceId}}"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
